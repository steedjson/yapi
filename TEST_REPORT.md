# 本轮工作区独立验证报告

- 日期：2026-09-16
- 验证范围：当前工作区本轮改动；未提交。
- 结论：**BLOCKED**（自动化与样式编译通过；全项目浏览器验收尚未完成）。

## 命令与结果

| 命令 | 结果 |
|---|---|
| `npm test` | 通过，345 tests passed，0 失败，0 跳过 |
| `npm run typecheck` | 通过 |
| `npx eslint`（本轮 JavaScript 文件） | 通过，0 error / 0 warning |
| `sass` 编译 Header、GroupList、Group、common SCSS | 全部通过；仅有现有 `@import` 弃用警告 |
| `git diff --check` | 通过 |

## 真实浏览器验收

- 启动前端：4000 端口。
- 启动后端：3000 端口。
- 修复并验证开发代理：4000 `/api/*` 转发到 3000。
- 修复并验证 SPA history fallback：刷新 `/group/14` 可加载前端页面。
- 390px 视口截图：左侧菜单可见，搜索框和选中态完整，页面不再出现底部横向滚动条。
- 原因确认：全局移动端 `html { width: min-content }` 与固定 `.g-row min-width` 导致溢出，已改为移动端宽度可收缩。
- 头部 DOM 导航入口仍存在；已改为移动端允许水平滚动，避免静默裁切。

## 尚未完成的正式验收项

以下项目尚无完整真实浏览器证据，不能宣称全项目已验收：

1. 未登录深链接 -> 登录 -> 原路径恢复（含 query/hash）。
2. 分组切换、刷新、浏览器前进/后退与时间线数据一致性。
3. 分组加载失败、空数据、无效 groupId 的完整可操作反馈。
4. Wiki/Markdown 页面视觉与交互回归。
5. 长文本接口表格、Tooltip 可访问性与横向滚动场景。
6. 桌面与移动端头部全部入口、用户菜单、折叠/滚动行为。
7. 全项目逐页面现代化视觉验收。

## 改动范围

当前工作区包含此前阶段与本轮的多类客户端、构建脚本、插件、静态资源和测试改动；未擅自清理或提交。正式交付前仍需逐项审查 diff，确认无越界改动。

## 最终结论

**BLOCKED**：核心路由、认证、分组竞态、开发代理、SPA 回退和移动端基础溢出已有代码与自动化证据；但用户要求的是整个项目的操作、UI 和现代化正式验收，当前证据仍不足，不能视为目标完成。

## 增量巡查验收记录（2026-09-16，监控会话接力轮）

本节由监控方（定期巡查任务）在监听会话停止后接力完成，仅追加不改写上文。

### 真实浏览器证据（本轮新增，环境 4000 前端 / 3000 后端，登录态 admin@admin.com）

- ✅ 深链接 `/group/14` 未登录/登录直载均正常（SPA fallback 复验通过）。
- ✅ 分组切换一致性：点击分组 "12"（_id 22）→ URL 同步 `/group/22`、左侧选中态/当前组名/项目列表三者一致；刷新后保持 22；浏览器后退回 `/group/14` 且三者同步回个人空间。
- ✅ 空分组反馈：分组 22 空列表时展示"该分组还没有项目呢"引导文案，无破版。
- ✅ 桌面 1280×720 回归：与改动前逐项一致，无回归。

### 本轮新发现并已修复的移动端缺陷（390×844 实测）

1. **头部纵向堆叠裁切**：`float:none` 后头部子块纵向堆叠成 192px，被 64px 头部垂直居中裁切，导航不可用。修复：`Header.scss` 移动端令 `.content.g-row` flex 单行布局、logo/面包屑改 flex 项（忽略内部浮动防塌陷）、`ul` 工具栏改 flex 单行、搜索框 100px/图标间距 6px；并压制 antd `Layout.Header` 默认 `padding: 0 50px`（移动端 `0 12px !important`，该项是 284px 内容盒装不下整行的根因）。
2. **主内容推出屏外不可达**：内层 Layout flex-basis auto 时 0 宽不触发换行，与全宽 Sider 同行溢出到 x≈400 屏外，且 body `overflow-x:hidden` 使其完全不可达。修复：`Group.scss` 移动端 `.projectGround .ant-layout-has-sider` 改 `flex-direction: column`（三级复合选择器压过 antd 同特异性运行时注入），Sider `flex: 0 0 auto !important`。修复后 Sider 全宽在上、项目列表 tabs 在下，整页纵向可用。

修复后证据：移动端整页截图（头部单行含 logo/搜索/图标/头像；侧栏与主内容纵向堆叠）、桌面回归截图；`npm test` 345 passed / 0 failed；`sass` 编译通过。

### 本轮后仍遗留

1. 未登录深链接→登录→原路径恢复的完整浏览器链：当前浏览器为 admin 登录态且无密码凭据，不能安全登出复验（逻辑侧已有 authRedirect 3 用例覆盖）。
2. 面包屑标题在移动端仅分得约 24px 宽，"个人空间"文字被裁（信息在侧栏冗余可见，属视觉打磨项）。
3. TimeLine 后退不刷新（`syncGroupSelection` 不派发 `fetchNewsData`）、news reducer 无竞态守卫、selectGroup"最新成功落地"语义——沿用上文第 23 节残留清单，未在本轮处理。
4. Wiki/Markdown 回归、长文本表格/Tooltip、全项目逐页视觉验收仍未开始。

## 增量巡查验收记录·第二轮（2026-09-16，监控会话接力）

上一节遗留清单的第 2、3 项本轮已修复并验证（csl-coder 实施 + 真实浏览器取证）：

### 修复内容

1. **TimeLine 动态不同步**：`client/components/TimeLine/TimeLine.js` 新增 `UNSAFE_componentWillReceiveProps`——typeid 变化（切组/前进/后退/改 URL）时重新拉取动态。同时 `GroupList.js` 的 `selectGroup` 移除直发新闻请求，避免与新机制双重请求。
2. **news reducer 竞态守卫 + 不可变化**：`client/reducer/modules/news.js` 复用 group.js 的递增序号模式（`newsRequestSequence`），过期响应忽略、errcode 非 0/异常 payload 不污染、列表改为不可变构造；`fetchNewsData`/`fetchMoreNews` 附带 `requestId`/`typeid`。
3. **移动端面包屑裁切**：`Header.scss` 面包屑容器 `min-width: 64px`，组名恢复可见。

### 验证结果（真实）

- 新增 `test/client/newsReducer.test.js` 5 用例（乱序忽略/失败不污染/正常排序/分页不可变/过期分页忽略）全部通过；全量 `npm test` **350 passed / 0 failed**；eslint、sass 编译通过。
- 真实浏览器（XHR 计数器取证）：打开分组动态 tab 后切组 → `/api/log/list` 恰好新增 1 次请求；后退 → 再 +1；前进 → 再 +1。每次导航恰好一次重拉，无双发（selectGroup 去重生效）。
- 移动端 390px 头部截图：logo、组名"12"、搜索框、3 个功能图标、头像+下拉单行完整可见。
- 桌面 1280×720 回归无变化。

### 截至本轮仍遗留

1. 未登录深链接→登录→原路径恢复完整浏览器链（需测试账号凭据，admin 登录态不可安全登出）。
2. selectGroup"最新成功落地获胜"语义（最新请求失败时 URL 与 currGroup 可能静默错位，无提示）。
3. Wiki/Markdown 回归、长文本表格/Tooltip 可访问性、全项目逐页视觉验收未开始。

## 增量巡查验收记录·第三轮（2026-09-16，监控会话接力）

本轮融资 Wiki/接口页取证，发现并修复一个 dev 环境缺陷：

### 新发现并已修复：Wiki 编辑器工具栏图标全部缺失

- 现象：项目 Wiki 页点"编辑"后，编辑器工具栏只剩分隔线痕迹，所有功能图标不可见。
- 根因：编辑器样式引用固定路径 `/common/tui-editor/dist/tui-editor(-2x).png`，本地未安装 tui-editor 包，dev server 对该路径返回 404。
- 修复：`build/webpack-dev-standalone.js` 新增静态别名 `/common/tui-editor/dist/ → static/prd/`（含路径穿越防护），并重启 dev server（4000 端口）生效。
- 验证：`curl` 雪碧图 200 image/png（原 404）；浏览器实测编辑器工具栏 16 个图标（标题/加粗/斜体/删除线/分隔线/引用/有序无序列表/任务/缩进/表格/图片/链接/行内代码/代码块）全部正常渲染。

### 其他取证结果

- **Wiki 页面**：`/project/28/wiki` 加载正常，项目导航 Wiki 项正确高亮，空状态干净；编辑器挂载成功，Write/Preview/Markdown/WYSIWYG 模式切换与 OK/Cancel 可用，未写入任何内容。
- **头部用户菜单**：头像下拉完整展示 个人中心/用户管理/系统信息/界面皮肤(子菜单)/退出，样式正常；已确认安全关闭，登录态未受影响。
- **接口页**：`/project/28` 路由重定向到 `/interface/api` 正常，项目导航（接口/动态/数据管理/设置/Wiki）渲染正确。
- **长文本表格/Tooltip 场景无法验证**：实例中项目 28 仅有空分类（`list: []`），项目 22 情况相同，**无接口测试数据**，该场景如实记录为"缺测试数据待验"，不做推测性结论。

### 测试与状态

- 新增 `test/build/webpack-dev-standalone.test.js` 雪碧图别名用例（200 image/png + PNG 魔数 + 缺失资源 404）；全量 `npm test` **351 passed / 0 failed**。
- dev server 已在本轮重启（4000 端口，后台进程），`/api` 代理与页面加载复验正常。
- 工作区 21 文件 +607/−205，均未提交。

### 截至本轮仍遗留

1. 未登录深链接→登录→原路径恢复完整浏览器链（需测试账号凭据）。
2. selectGroup"最新成功落地获胜"语义（最新请求失败时 URL 与 currGroup 可能静默错位，无提示）。
3. 长文本表格/Tooltip 场景（需先造接口测试数据，或由用户提供真实数据）。
4. 全项目逐页面视觉回归（分组/项目主干已覆盖，设置页/数据管理页/动态页待扫）。

## 增量巡查验收记录·第四轮（2026-09-16，监控会话接力）

本轮融资剩余页面的逐页扫描（桌面 1280×720，登录态 admin），全部 DOM 级事实核验：

| 页面 | 结果 |
|---|---|
| /project/28/setting | ✅ 11 个表单项（项目ID/名称/分组/基本路径/MOCK地址/描述/tag/mock严格模式）渲染完整 |
| /project/28/data | ✅ 数据导入页完整（Swagger v2.0+/数据同步/完全覆盖/url导入/拖拽上传/命令行导入） |
| /project/28/activity | ✅ 正常渲染，含 Api 筛选与空态提示 |
| /follow | ✅ 空态引导"你还没有关注项目呢"友好 |
| /add-project | ✅ 新建项目表单完整（名称/分组/路径/描述/权限/创建按钮） |
| / （登录态） | ✅ **正确重定向到 /group/14**（Home 登录态重定向修复的实机验证） |
| 无效路径 /project/28/settings | ✅ 正确渲染"页面不存在"引导页（错误态反馈达标；正确地址为单数 /setting） |

### 状态

- 全量 `npm test` 351 passed / 0 failed（上轮后无代码变更，本轮为纯取证）。
- 工作区 21 文件 +607/−205，无 commit，与上轮一致。

### 逐页验收结论

分组页、项目五个主页面、关注、新建项目、首页重定向、404 错误态、移动端 390 头部与布局均已有真实浏览器证据；"全项目逐页视觉回归"的结构性覆盖完成。剩余未闭环项均依赖外部输入：

1. 未登录深链接→登录→恢复链：**需测试账号凭据**。
2. 长文本表格/Tooltip：**需接口测试数据**（涉及向 dev 库造数，待授权）。
3. selectGroup"最新成功落地获胜"语义（边缘场景，待产品裁决是否提示失败）。
